name: votacao
recipe: drupal11
config:
  webroot: web
  php: '8.3'
  composer_version: '2'
  database: mysql:8.0
  xdebug: false
tooling:
  phpunit:
    service: appserver
    cmd: vendor/bin/phpunit -c phpunit.xml
    description: Run PHPUnit tests
