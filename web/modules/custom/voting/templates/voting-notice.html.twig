{#
/**
 * @file
 * A short message about the state of the vote for the current user.
 *
 * Available variables:
 * - message: the message, may contain safe markup (links).
 * - type: one of info, success, warning.
 */
#}
<p class="notice notice--{{ type }}">{{ message }}</p>
